/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/github-pages',
  // assetPrefix: '/github-pages',
  // output: 'export',
  // images: {
  //   unoptimized: true, 
  // },
};

export default nextConfig;
